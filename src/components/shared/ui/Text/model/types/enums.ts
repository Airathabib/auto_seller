export enum TextType {
	TITLE = 'title',
	TEXT = 'text',
}

export enum TitleTag {
	H1 = 'h1',
	H2 = 'h2',
	H3 = 'h3',
	H4 = 'h4',
}

export enum TextTag {
	P = 'p',
	SPAN = 'span',
	DIV = 'div',
}

export enum TextSize {
	XS = '--text-xs', // 12px
	S = '--text-sm', // 14px
	M = '--text-base', // 16px
	L = '--text-lg', // 18px
	XL = '--text-xl', // 20px
	XXL = '--text-2xl', // 24px
	SIZE_28 = '--text-28',
	SIZE_30 = '--text-30',
	SIZE_34 = '--text-34',
	SIZE_40 = '--text-40',
	SIZE_60 = '--text-60',
}

export enum FontWeight {
	LIGHT = '300',
	MEDIUM = '500',
	BOLD = '700',
}

export enum TextAlign {
	LEFT = 'left',
	CENTER = 'center',
	RIGHT = 'right',
	JUSTIFY = 'justify',
}

export enum TextClamp {
	LINES_2 = 2,
	LINES_3 = 3,
}

export enum TextColor {
	WHITE = '--base-white',
	BLACK = '--base-black',
	BLACK_1 = '--base-black-1',
	BLACK_2 = '--base-black-2',
	GRAY = '--base-gray',
	ORANGE = '--base-orange',
	GREEN = '--status-online',
}
